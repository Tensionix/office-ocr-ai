Place project secrets here as PLAIN TEXT (one value per file, no YAML):
  yandex_key.txt      -> Yandex Cloud API key (or IAM token)
  yandex_folder.txt   -> Yandex Cloud folder id
File names must match ocr.engines.json -> yandex.creds.{secret_file,folder_id_file}.
Never commit real secrets.
