[pytest]
testpaths = tests
norecursedirs = runtime wheelhouse ._runtime output report logs release workspace cache tmp PDF
markers =
    integration: local end-to-end smoke using bundled project tools

# NiceGUI's user fixture otherwise looks for a main.py to import. This app hands
# build_ui to ui.run(root=...), so no page exists at import time and the GUI tests
# register it themselves.
main_file =
